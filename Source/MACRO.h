#pragma once

#define GC GreenOneCharacter

#define INT_ZERO 0	
#define FLOAT_ZERO 0.f

#define SI(si) (si)
#define ALORS(alors) (alors)
#define SINON(sinon) (sinon)
#define TERNAIRE(SI, ALORS, SINON) (SI ? ALORS : SINON)
